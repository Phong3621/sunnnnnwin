# Bot requirements
pytelegrambotapi==4.8.0

# Client requirements (giảm bớt dependencies cho server)
requests==2.31.0

# Server requirements
flask==2.3.3
flask-cors==4.0.0
cryptography==41.0.7

# Common
python-dotenv==1.0.0

# Loại bỏ customtkinter và Pillow vì không cần trên server
